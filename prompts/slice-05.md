Follow the repo protocol: read docs/architecture.md (v0.1),
docs/slices/05-portability/brief.md and progress.md. Session plan first.
This session: BackupV1 and the RFC-4180 reader/writer with round-trip tests. Before anything else, check fixtures/imports/ for both sample files; if either is missing, write the question in progress.md and stop for a ruling.
Questions and propose-first items go to progress.md — no implementation
until ruled. Write your handoff to docs/slices/05-portability/handoffs/session-S.md
— the FULL path; a bare relative name lands at the repo root. Never state
that the gate will pass: state what you changed and why you believe it
will; the gate decides. Never touch protected paths.
