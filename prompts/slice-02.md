Follow the repo protocol: read docs/architecture.md (v0.1),
docs/slices/02-domain-units-money/brief.md and progress.md. Session plan first.
This session: core.units and core.money with tests, then the model, default record types, and validation.
Questions and propose-first items go to progress.md — no implementation
until ruled. Write your handoff to docs/slices/02-domain-units-money/handoffs/session-S.md
— the FULL path; a bare relative name lands at the repo root. Never state
that the gate will pass: state what you changed and why you believe it
will; the gate decides. Never touch protected paths.
Adding a dependency is out of scope for this slice — if one seems needed,
it is an open question.
